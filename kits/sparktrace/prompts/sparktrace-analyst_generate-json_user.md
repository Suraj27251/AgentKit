# Analyst Input

Symptom:
{{triggerNode_1.output.symptom}}

Hypothesis under test (JSON):
{{triggerNode_1.output.hypothesis}}

Query that was run (JSON):
{{triggerNode_1.output.query}}

Compact result digest (JSON — CompactResult: sample rows + stats, not raw rows):
{{triggerNode_1.output.result}}

Produce the verdict now, following the output contract exactly.
