Symptom reported by the on-call engineer:
{{triggerNode_1.output.symptom}}

Pipeline context (JSON — files, tables, dag, summary):
{{triggerNode_1.output.pipeline}}

Evidence gathered so far this investigation (JSON — PlannerEvidence[]):
{{triggerNode_1.output.evidence}}

Hypotheses proposed so far, with current status (JSON — Hypothesis[]):
{{triggerNode_1.output.hypothesesTried}}

Decide the single best next action now, following the output contract exactly.
